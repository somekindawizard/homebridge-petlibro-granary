{
  "pluginAlias": "PetLibro",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Homebridge plugin for PETLIBRO smart pet devices.\n\n**Important:** PETLIBRO only allows one session per account. If you keep the PETLIBRO mobile app signed in alongside Homebridge, the plugin will be silently logged out (and vice versa). The recommended setup is to **create a dedicated PETLIBRO account just for Homebridge** and share your devices to it from your primary account.\n\nReview [Account Management](https://github.com/jjjonesjr33/petlibro/wiki/PetLibro-Account-Management) and [Password Limitations](https://github.com/jjjonesjr33/petlibro/wiki/PetLibro-Password-Limitation) for caveats.",
  "footerDisplay": "For support, see the [GitHub issues](https://github.com/somekindawizard/homebridge-petlibro-granary/issues).",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "PetLibro",
        "required": true
      },
      "email": {
        "title": "PETLIBRO Account Email",
        "type": "string",
        "format": "email",
        "required": true,
        "description": "Email address used to log in to PETLIBRO. Strongly recommended to be a dedicated account, not your primary one."
      },
      "password": {
        "title": "PETLIBRO Account Password",
        "type": "string",
        "required": true,
        "description": "Stored in plaintext in your Homebridge config. The plugin MD5-hashes it before sending to the API, but the original is needed to log in. Use a unique password for this account."
      },
      "region": {
        "title": "Region",
        "type": "string",
        "default": "US",
        "required": true,
        "oneOf": [
          { "title": "United States", "enum": ["US"] }
        ]
      },
      "pollIntervalSeconds": {
        "title": "Poll Interval (seconds)",
        "type": "integer",
        "default": 60,
        "minimum": 30,
        "maximum": 600,
        "description": "Fast-tier polling cadence for live state (food level, online, etc.). Slow-tier endpoints (feeding plan, OTA, attribute settings) refresh every 5 minutes regardless. After any user action, polling temporarily speeds up to 15s for 2 minutes."
      },
      "manualFeedPortions": {
        "title": "Manual Feed Portions",
        "type": "integer",
        "default": 2,
        "minimum": 1,
        "maximum": 48,
        "description": "Number of portions to dispense when the Manual Feed switch is triggered. 1 portion ≈ 1/12 cup on standard feeders."
      },
      "desiccantCycleDays": {
        "title": "Desiccant Cycle (days)",
        "type": "integer",
        "default": 30,
        "minimum": 7,
        "maximum": 180,
        "description": "How many days a fresh desiccant pack lasts. Used to compute the FilterLifeLevel percentage shown in the Eve app and similar."
      },
      "debug": {
        "title": "Debug Logging",
        "type": "boolean",
        "default": false,
        "description": "Enable verbose debug logs. Useful when reporting issues."
      }
    }
  }
}
